// import nodeFS from 'fs';
// import nodePath from 'path';

// export const fs = window ? window.NodeModule.fs : nodeFS;
// export const path = window ? window.NodeModule.path : nodePath;

// export const fs = window.